const state = {
  headerComponentId: 0,
};

const mutations = {
  SET_HEADER_COMPONENT_ID(state, id) {
    state.headerComponentId = id;
  }
};

const actions = {
};

export default {
  state,
  mutations,
  actions,
};
